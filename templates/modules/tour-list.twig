<div class="tour-list row">
    {% for item in content.tour_list %}
        <div class="col-12 col-md-6 col-lg-3">
            {% include "tease-tour.twig" with { tour: item.tour, img_type: "thumbnail" } %}
        </div>
    {% endfor %}
</div>
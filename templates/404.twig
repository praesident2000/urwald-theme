{% extends "base.twig" %}

{% block content %}
	<article class="{{post.post_type}}" id="post-{{post.ID}}">
        <section class="{{post.post_type}}__content content__no-hero">
			<div class="container">
				<div class="row">
					<div class="col-12 col-lg-8 offset-lg-2">
						<h2>Oh da ist wohl etwas schief gegangen.</h2>
					<div class="col-12 col-lg-6 offset-lg-3">
						<div class="cta">
							<a href="{{ site.url }}" class="cta__link btn btn--rounded btn--cta" >Zurück zur Startseite</a>
						</div>
					</div>
				</div>
			</div>

        </section>
    </article>
{% endblock %}